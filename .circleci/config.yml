version: 2.1

jobs:
    build:
        working_directory: ~/zilean/plot-service
        docker:
            - image: "cimg/python:3.8.10"
        steps:
            - checkout
            - run: cd plot-service && pip install -r requirements.txt && pytest tests